<?xml version="1.0" encoding="UTF-8"?>
<!--
 * Copyright 2009-2016 the CodeLibs Project and the Others.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND,
 * either express or implied. See the License for the specific language
 * governing permissions and limitations under the License.
-->
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             web-app_6_1.xsd"
         version="6.1">

  <!-- ================================================================================= -->
  <!--                                                                    Servlet Filter -->
  <!--                                                                    ============== -->
  <filter>
    <filter-name>encodingFilter</filter-name>
    <filter-class>org.codelibs.fess.filter.EncodingFilter</filter-class>
    <init-param>
      <param-name>encoding</param-name>
      <param-value>UTF-8</param-value>
    </init-param>
    <init-param>
      <param-name>encodingRules</param-name>
      <param-value>sjis:Shift_JIS,eucjp:EUC-JP</param-value>
    </init-param>
  </filter>

  <filter>
    <filter-name>rateLimitFilter</filter-name>
    <filter-class>org.codelibs.fess.filter.RateLimitFilter</filter-class>
  </filter>

  <filter>
    <filter-name>loadControlFilter</filter-name>
    <filter-class>org.codelibs.fess.filter.LoadControlFilter</filter-class>
  </filter>

  <filter>
    <filter-name>corsFilter</filter-name>
    <filter-class>org.codelibs.fess.filter.CorsFilter</filter-class>
  </filter>

  <filter>
    <filter-name>webApiFilter</filter-name>
    <filter-class>org.codelibs.fess.filter.WebApiFilter</filter-class>
  </filter>

  <filter>
    <filter-name>lastaPrepareFilter</filter-name>
    <filter-class>org.lastaflute.web.servlet.filter.LastaPrepareFilter</filter-class>
  </filter>

  <filter>
    <filter-name>staticThemeFilter</filter-name>
    <filter-class>org.codelibs.fess.filter.StaticThemeFilter</filter-class>
  </filter>

  <filter>
    <filter-name>lastaShowbaseFilter</filter-name>
    <filter-class>org.lastaflute.web.servlet.filter.LastaShowbaseFilter</filter-class>
    <init-param>
      <param-name>errorLogging</param-name>
      <param-value>false</param-value>
    </init-param>
  </filter>

  <filter>
    <filter-name>lastaToActionFilter</filter-name>
    <filter-class>org.lastaflute.web.servlet.filter.LastaToActionFilter</filter-class>
  </filter>

  <filter>
    <filter-name>httpHeaderSecurity</filter-name>
    <filter-class>org.apache.catalina.filters.HttpHeaderSecurityFilter</filter-class>
    <init-param>
      <param-name>blockContentTypeSniffingEnabled</param-name>
      <param-value>true</param-value>
    </init-param>
    <init-param>
      <param-name>hstsEnabled</param-name>
      <param-value>false</param-value>
    </init-param>
    <init-param>
      <param-name>antiClickJackingEnabled</param-name>
      <param-value>false</param-value>
    </init-param>
  </filter>

  <!-- ================================================================================= -->
  <!--                                                                    Filter Mapping -->
  <!--                                                                    ============== -->
  <filter-mapping>
    <filter-name>encodingFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
    <dispatcher>FORWARD</dispatcher><!-- TODO -->
  </filter-mapping>

  <filter-mapping>
    <filter-name>lastaPrepareFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
    <dispatcher>FORWARD</dispatcher>
    <dispatcher>INCLUDE</dispatcher>
  </filter-mapping>

  <filter-mapping>
    <filter-name>httpHeaderSecurity</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <filter-mapping>
    <filter-name>rateLimitFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <filter-mapping>
    <filter-name>loadControlFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <filter-mapping>
    <filter-name>corsFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <!--
    SECURITY (C-1 / PR #3136): staticThemeFilter MUST come AFTER httpHeaderSecurity,
    rateLimitFilter, loadControlFilter, and corsFilter on the REQUEST dispatch.

    staticThemeFilter intercepts GET requests and RequestDispatcher.forward()s them
    to /theme-view. Filters mapped later in the chain that only declare
    <dispatcher>REQUEST</dispatcher> will NOT run on the forwarded request, so any
    filter required to apply to static-theme asset/index responses must be ordered
    before this mapping. Reordering — rather than adding FORWARD dispatcher scopes
    everywhere — preserves the existing behavior of those security filters on the
    REQUEST chain (rate-limiting per-request, HSTS/clickjacking headers, CORS,
    load shedding) without risking double-application via FORWARD.

    lastaPrepareFilter retains REQUEST+FORWARD so the forwarded ThemeViewAction
    still has Lasta context. Do not move staticThemeFilter ahead of these filters.
  -->
  <filter-mapping>
    <filter-name>staticThemeFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <filter-mapping>
    <filter-name>webApiFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <filter-mapping>
    <filter-name>lastaShowbaseFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <filter-mapping>
    <filter-name>lastaToActionFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <filter-mapping>
    <filter-name>lastaToActionFilter</filter-name>
    <url-pattern>/theme-view</url-pattern>
    <dispatcher>FORWARD</dispatcher>
  </filter-mapping>

  <!-- ================================================================================= -->
  <!--                                                                           Servlet -->
  <!--                                                                           ======= -->

  <!-- ================================================================================= -->
  <!--                                                                   Servlet Mapping -->
  <!--                                                                   =============== -->

  <!-- ================================================================================= -->
  <!--                                                                  Servlet Listener -->
  <!--                                                                  ================ -->
  <listener>
    <listener-class>org.apache.commons.fileupload2.jakarta.servlet6.JakartaFileCleaner</listener-class>
  </listener>

  <!-- ================================================================================= -->
  <!--                                                                    Session Config -->
  <!--                                                                    ============== -->
  <!--
    SECURITY: Session cookie hardening (MJ-3 / PR #3136).

    HttpOnly  - blocks JavaScript access to JSESSIONID, mitigating XSS-based session theft.
    Secure    - restricts cookie transmission to HTTPS only.

    PRODUCTION REQUIREMENT: Fess MUST be served over HTTPS (TLS termination at the
    container, a reverse proxy, or a load balancer).  If you run HTTP-only (e.g. plain
    `bin/fess` on port 8080 without TLS), the browser will silently drop the Secure cookie
    and sessions will not persist.  For HTTP-only local development either:
      (a) front Fess with an HTTPS reverse proxy (nginx/Caddy with a self-signed cert), or
      (b) temporarily remove the <secure> element — but do NOT do this in production.

    SameSite policy cannot be set via web.xml in Servlet 6.1 / Jakarta EE 10.
    To enforce SameSite=Lax (recommended) configure the Tomcat CookieProcessor at the
    deployment level in one of:
      - META-INF/context.xml  (per-webapp, checked into the WAR):
            <Context>
              <CookieProcessor sameSiteCookies="lax"/>
            </Context>
      - $CATALINA_HOME/conf/context.xml  (global, all webapps)
      - A reverse proxy that rewrites Set-Cookie headers.
    See https://tomcat.apache.org/tomcat-10.1-doc/config/cookie-processor.html
  -->
  <session-config>
    <cookie-config>
      <http-only>true</http-only>
      <secure>true</secure>
    </cookie-config>
    <tracking-mode>COOKIE</tracking-mode>
  </session-config>

  <!-- ================================================================================= -->
  <!--                                                                      Welcome File -->
  <!--                                                                      ============ -->
<!--
    <welcome-file-list>
    </welcome-file-list>
  -->

  <!-- ================================================================================= -->
  <!--                                                                        JSP Config -->
  <!--                                                                        ========== -->
  <jsp-config>
    <jsp-property-group>
      <url-pattern>*.jsp</url-pattern>
      <el-ignored>false</el-ignored>
      <page-encoding>UTF-8</page-encoding>
      <scripting-invalid>false</scripting-invalid>
      <include-prelude>/WEB-INF/view/common/common.jsp</include-prelude>
    </jsp-property-group>
  </jsp-config>

  <!-- ================================================================================= -->
  <!--                                                                        Error Page -->
  <!--                                                                        ========== -->
  <error-page>
    <error-code>400</error-code>
    <location>/WEB-INF/view/error/redirect.jsp?type=badRequest</location>
  </error-page>
  <error-page>
    <error-code>401</error-code>
    <location>/WEB-INF/view/error/redirect.jsp?type=badAuth</location>
  </error-page>
  <error-page>
    <error-code>403</error-code>
    <location>/WEB-INF/view/error/redirect.jsp?type=logOut</location>
  </error-page>
  <error-page>
    <error-code>404</error-code>
    <location>/WEB-INF/view/error/redirect.jsp?type=notFound</location>
  </error-page>
  <error-page>
    <error-code>408</error-code>
    <location>/WEB-INF/view/error/redirect.jsp?type=logOut</location>
  </error-page>
  <error-page>
    <error-code>429</error-code>
    <location>/WEB-INF/view/error/redirect.jsp?type=busy</location>
  </error-page>
  <error-page>
    <error-code>500</error-code>
    <location>/WEB-INF/view/error/redirect.jsp?type=systemError</location>
  </error-page>
</web-app>
